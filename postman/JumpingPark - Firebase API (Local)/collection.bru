meta {
  name: JumpingPark - Firebase API (Local)
}

auth {
  mode: none
}

vars:pre-request {
  baseUrl: http://localhost:3000
  authToken: 
  lastCreatedId: 
}

docs {
  Colección de pruebas para los endpoints server (Next.js App Router) creados en /api/*.
}
